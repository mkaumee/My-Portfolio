/**
 * Data exports for the portfolio application
 * Centralizes all data imports for easy access throughout the application
 */

export { 
  portfolioData, 
  animationConfig, 
  seoConfig, 
  themeConfig 
} from './portfolio';

export type { PortfolioData } from '../types';